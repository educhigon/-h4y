<div class="post-image">
  <% if post.photos.count > 0 %>
    <% post.photos.each do |i| %>
      <%= cl_image_tag(i.key,
      width: 400, height: 300, crop: :fill) %>
    <% end %>
  <% else %>
    <%= image_tag "H4you.png", :class => "post-image"%>
  <% end %>
</div>
